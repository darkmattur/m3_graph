[pytest]
python_classes = Test*
python_functions = test_*
asyncio_mode = auto

# Output options
addopts =
    -v
    --tb=short

# Test discovery
norecursedirs = .git .venv venv __pycache__ *.egg-info
